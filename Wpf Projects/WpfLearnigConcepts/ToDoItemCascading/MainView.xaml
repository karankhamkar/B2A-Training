<Window x:Class="ToDoItemCascading.MainView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="ToDoItem" Height="450" Width="550">
    <Grid Margin="70">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <TextBlock Text="Welcome to My To Do Items Application..!"
                   FontSize="15"
                   FontWeight="Bold"
                   Grid.Row="0"/>
        <Button Command="{Binding AddToDoItemCommand}"
                 Content="Add ToDo Item"
                 HorizontalAlignment="Right"
                 Margin="0,10"
                 Grid.Row="1"
                 Width="100"/>
        <DataGrid ItemsSource="{Binding ToDoItemList}"
                   Grid.Row="2"/>
    </Grid>
</Window>
